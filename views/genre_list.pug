extends layout

block content
  h1= title

  ul(class="list")
    each genre in genre_list
      li
        a(href=genre.url) 
          div(class="game-item")
            div #{genre.name} 

    else
      li There are no genres.